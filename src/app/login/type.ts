export type UserMetaData = {
  uid: string;
  email: string;
  displayName: string;
  photoURL: string;
  storagePath: string;
};

export type GithubUserInfo = {
  displayName: string;
  photoURL: string;
};
